// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Rack {
  id Int @id @default(autoincrement())
  code String @unique
  Switchs Switch[]
  PatchPanels Patchpanel[]
}

model Switch {
  id Int @id @default(autoincrement())
  code String @unique
  rackCode String
  rack Rack @relation(fields: [rackCode], references: [code])
  Ports Port[]
}

model Patchpanel {
  id Int @id @default(autoincrement())
  code String @unique
  rackCode String
  rack Rack @relation(fields: [rackCode], references: [code])
  patchports Patchport[]
}

model Patchport {
  id Int @id @default(autoincrement())
  patchcode String
  patchpanel Patchpanel @relation(fields: [patchcode], references: [code])
  switchportId Int
  switchport Port @relation(fields: [switchportId], references: [id])
}

model Port {
  id Int @id @default(autoincrement())
  code String
  switchCode String
  switch Switch @relation(fields: [switchCode], references: [code])
  desc String @default("")
  department Department @relation(fields: [departId], references:[id])
  departId Int @default(0)
  patchport Patchport?
}

model Department {
  id Int @id @default(autoincrement())
  departName String
  Ports Port[]
}